# Dart / Flutter
.dart_tool/
.packages
build/
pubspec.lock
*.iml

# Vendored upstream snapshots — refetch with tools/fetch-40kdc.sh and
# tools/fetch-bsdata.sh. Neither is committed: they are large, they are
# reproducible from a pinned revision, and what ships is the derived bundle.
data/40kdc/
data/bsdata/

# Built by tools/merge — BSData over 40kdc (DESIGN.md §3.10).
data/merged/

# macOS
.DS_Store

# Flutter build output
packages/wh40k_app/.dart_tool/
packages/wh40k_app/build/
packages/wh40k_app/ios/Flutter/ephemeral/
packages/wh40k_app/ios/Pods/
packages/wh40k_app/.flutter-plugins-dependencies

# Built dataset bundles — regenerate with packages/wh40k_core/bin/bundle.dart
dist/

# Cross-check source — refetch with tools/fetch-mfm.sh
data/mfm/
